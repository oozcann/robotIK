import bootbox from 'bootbox';
export const MainStates = function ($stateProvider) {
	return (
		$stateProvider
			/*
			.state('home', {
				url: '/home',
				template: '<h1>Onur</h1>',
				controller: [() => {
				    bootbox.alert('deneme');
				    //window.alert('test');
				    console.log(moment());
				}]
			})
			*/
	);
};